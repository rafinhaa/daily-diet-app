export type SignInDataParams = { email: string; password: string };

export type SignInData = {
  user: {
    avatar_url: string;
    created_at: string;
    email: string;
    id: string;
    name: string;
    updated_at: string;
  };
};
